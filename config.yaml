SWAGGER_HOST:
SWAGGER_URL:

app:
    server:
        port: 8000
        # false:debugMode, true:releaseMode
        gin-release: fasle
    db:
        dbms: postgres
        host: localhost
        port: 5432
        db: mailer
        user: postgres
        password: 1234
    smtp:
        host: host
        port: 1234
        user: user
        password: password
        vertification: false
